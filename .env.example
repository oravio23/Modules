# Shared Supabase project — one project, one JWT issuer, so every module shares a session.
# Never put a service-role key behind a VITE_ prefix; it must stay server-side only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
