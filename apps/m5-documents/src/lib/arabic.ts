// Canonical implementation lives in supabase/functions/_shared/arabic.ts —
// shared verbatim with the edge functions via a relative import, so the
// review pane's RTL detection and the pipeline's evidence-anchoring gate
// normalise text identically.
export * from "../../../../supabase/functions/_shared/arabic.ts";
