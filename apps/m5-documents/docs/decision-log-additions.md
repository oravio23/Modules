# Decision Log Additions — Oravio M5 Document Intelligence Implementation

Continues `decision-risk-artifact-register.xlsx` → **Decision Log** tab from D-006. Same row format;
paste directly into that tab once a live copy is available. All statuses are **PROPOSED** —
recorded because an engineering decision had to be made to proceed, per CLAUDE.md §6's instruction
to log rather than silently settle open questions.

| Decision ID | Decision | Rationale / Source | Decided by | Date | Status | Downstream impact |
|---|---|---|---|---|---|---|
| D-007 | Scope this build as a generic any-file-type engine plus a pluggable document-profile layer, with `commercial_invoice` as the first governed profile and `generic` as a fallback for anything else. | User request ("any input... doesn't matter what file type") conflicts on its face with Charter §4's commercial-invoice-only bounded scope. Resolved by making the bound apply to the *governed, validated* profile, not to what the ingest pipeline can accept. | Claude (AI drafting/implementing assistant), per task instruction | 2026-08-06 | PROPOSED — awaiting Product Owner / Domain Reviewer confirmation (both seats unnamed) | Adding Bill of Lading, Packing List, or Certificate of Origin (Charter §3's eventual scope) becomes a new profile + fixtures, not a pipeline rewrite. `generic` profile has no field-level validators beyond evidence/completeness — never treat its output as customs-filing-ready. |
| D-008 | Adopt Vite + React + TypeScript + Tailwind + shadcn/ui + Supabase as the technology stack, matching what Lovable generates, per explicit user request to migrate there for hosting. | User instruction + ADR-012. Confirms the "modular monolith" option in ADR-001 (still itself PROPOSED). | Claude, per task instruction | 2026-08-06 | PROPOSED — awaiting Engineering Lead (unnamed) | Repository host, default branch, and CI provider remain unselected (Task Protocol §1 [OPEN] item) — this decision only fixes the application stack, not deployment/CI tooling. |
| D-009 | Author a PROPOSED Result-Envelope JSON Schema, validator catalogue, and commercial-invoice field catalogue from first principles, since the real v0.1/v0.3 artifacts were not present in the delivered source package. | CLAUDE.md §5's own admission that these files don't exist in the delivered package, combined with the instruction that a task needing an unresolved input should make an explicit proposal rather than block entirely. | Claude, per task instruction | 2026-08-06 | PROPOSED — awaiting Engineering Lead / Domain Reviewer | See ADR-013. All three artifacts are designed to be swapped for the real ones as data (profile rows, schema file, validator ID list) without changing pipeline code — swap-and-rerun `npm test` is the intended reconciliation path. |
| D-010 | Confirm JSON Schema Draft 2020-12 (already retroactively logged as D-005) as the dialect actually implemented, via `ajv`'s `Ajv2020` build. | Continuation of D-005; a concrete choice had to be made to write working `ajv` validation code. | Claude, per task instruction | 2026-08-06 | PROPOSED — awaiting Engineering Lead | See ADR-014. If the real Result-Envelope file specifies a different dialect, that's a direct conflict to resolve, not something this repo silently reconciled. |
| D-011 | Enforce evidence anchoring as a deterministic, model-independent gate (pure TypeScript, no API calls) rather than relying on Claude's native `citations` feature or self-reported confidence. | Charter core policy (no unsupported critical output reaches a downstream system) + Task Protocol §7 (validation separate from the model) + technical constraint: `citations` is incompatible with the structured-output tool use this pipeline needs. | Claude, per task instruction | 2026-08-06 | PROPOSED — awaiting Engineering Lead / Domain Reviewer | See ADR-013. This is the mechanism the "never silently invent a value" policy actually rests on in this implementation — any future change to the extraction approach (e.g. adopting a different model or provider) must preserve an equivalent independent evidence check, not assume the model's own citation behavior is sufficient. |
| D-012 | Hardcode `requires_review = true` unconditionally for every field during the pilot, rather than deriving it from confidence or validator outcome. | Charter §5 ("100% of outputs pass through mandatory human review during the pilot period; no field-level auto-population is enabled until empirically justified") and Task Protocol's framing of pilot auto-population as a policy decision, not an engineering default. | Claude, per task instruction | 2026-08-06 | PROPOSED — awaiting Exec. Sponsor / Release Approver (this is explicitly framed as a pilot-policy decision in the source material, not a pure engineering call) | Any future move away from 100% human review (e.g. auto-approving high-confidence, fully-anchored fields) requires an explicit, separate approval per the framework's own rule — this repo does not build a code path for that today. |
