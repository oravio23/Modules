export function SiteFooter() {
  return (
    <footer className="border-t border-[var(--app-line)] px-[clamp(18px,4vw,56px)] py-6 text-center text-xs text-[var(--app-text-muted)]">
      Pilot build — every module here is under active development. Synthetic data only until
      a module is marked live in production.
    </footer>
  );
}
